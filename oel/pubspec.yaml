name: oel
description: A university lab project for managing academic tasks.
version: 1.0.0+1
publish_to: 'none' 

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Requirement 5: Local Data Storage
  shared_preferences: ^2.3.5 

  # Optional: For better icon sets
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true